// -------------------------------------
// WARNING
// THIS FILE IS AUTOGENERATED BY PRISMERGE
// DO NOT MANUALLY EDIT THIS FILE!
// -------------------------------------

// supabase setup
generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"] // Add this line
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
  schemas   = ["originstack-supabase-schema"] // Add this line
}

model Author {
  id    Int         @id @default(autoincrement())
  name  String      @unique
  posts QuotePost[] // relation to QuotePost

  @@schema("originstack-supabase-schema")
}

model QuotePost {
  id        Int      @id @default(autoincrement())
  content   String
  views     Int      @default(0)
  createdAt DateTime @default(now())
  author    Author   @relation(fields: [authorId], references: [id])
  authorId  Int

  @@schema("originstack-supabase-schema")
}
